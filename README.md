# Pure-Python LL(1) Lexer and Parser

See the [examples](examples/) and [unit tests](tests/parser_test.py) for usage examples.

The examples include an interpreter for a simple dialect of the BASIC programming language, and a compiler from BASIC into C++.